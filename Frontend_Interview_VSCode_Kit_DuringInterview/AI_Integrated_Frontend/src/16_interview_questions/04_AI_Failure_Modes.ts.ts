/**
 * INTERVIEW ONE-LINER:
 * Senior AI frontend engineers plan for hallucinations, malformed output, prompt injection, tool failure, stale retrieval, partial streams, timeouts, model drift, rate limits, and inaccessible AI UI states.
 *
 * STUDY METHOD:
 * 1. Say the one-liner aloud.
 * 2. Read the comments beside the implementation.
 * 3. Run or integrate the example using the README instructions.
 */


const failureModes = [
  "hallucination",
  "schema mismatch",
  "prompt injection",
  "tool timeout",
  "retrieval miss",
  "partial stream",
  "rate limit",
  "model behavior drift",
  "unsafe rendered HTML",
  "accessibility failure"
];

console.log(failureModes);

/**
 * INTERVIEW ONE-LINER:
 * A workflow has mostly predefined steps, while an agent dynamically chooses actions/tools; prefer deterministic workflows unless open-ended decision-making genuinely adds value.
 *
 * STUDY METHOD:
 * 1. Say the one-liner aloud.
 * 2. Read the comments beside the implementation.
 * 3. Run or integrate the example using the README instructions.
 */


const comparison = {
  workflow: "Predictable sequence controlled by application code",
  agent: "Model chooses next actions/tools within bounded permissions"
};

console.log(comparison);

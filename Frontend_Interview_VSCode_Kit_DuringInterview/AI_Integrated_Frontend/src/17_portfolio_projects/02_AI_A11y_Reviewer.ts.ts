/**
 * INTERVIEW ONE-LINER:
 * An AI accessibility reviewer combines deterministic DOM/a11y checks with model explanation so AI augments standards-based testing instead of replacing it.
 *
 * STUDY METHOD:
 * 1. Say the one-liner aloud.
 * 2. Read the comments beside the implementation.
 * 3. Run or integrate the example using the README instructions.
 */


const pipeline = [
  "Run deterministic accessibility checks",
  "Collect violations + DOM context",
  "Ask model to explain/fix",
  "Return structured file/selector/severity suggestions",
  "User reviews patch"
];

console.log(pipeline);

/**
 * INTERVIEW ONE-LINER:
 * CORS controls browser cross-origin read permissions, while CSRF is an authenticated cross-site request attack; they are different mechanisms.
 *
 * RUN:
 * node src/20_security_architecture/03_CSRF_CORS.js
 *
 * NOTE:
 * Important statements are explained inline so this file can be used for
 * revision, live coding practice, and interview screen-sharing.
 */


const concepts = {
  CORS: "Browser policy controlling cross-origin request/response access",
  CSRF: "Attacker causes a victim browser to send an authenticated state-changing request",
};

console.log(concepts);

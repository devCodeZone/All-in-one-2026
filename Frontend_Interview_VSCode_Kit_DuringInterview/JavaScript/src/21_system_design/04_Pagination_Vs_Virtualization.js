/**
 * INTERVIEW ONE-LINER:
 * Pagination limits how much data is fetched, while virtualization limits how many DOM rows are rendered; large tables may need both.
 *
 * RUN:
 * node src/21_system_design/04_Pagination_Vs_Virtualization.js
 *
 * NOTE:
 * Important statements are explained inline so this file can be used for
 * revision, live coding practice, and interview screen-sharing.
 */


const concepts = {
  pagination: "Network/data-volume optimization",
  virtualization: "DOM/rendering optimization",
  cursorPagination: "Stable incremental pagination for changing feeds",
};

console.log(concepts);

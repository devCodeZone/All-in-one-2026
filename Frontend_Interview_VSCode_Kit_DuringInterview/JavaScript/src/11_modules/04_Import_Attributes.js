/**
 * INTERVIEW ONE-LINER:
 * Import attributes attach host-understood metadata to imports, with JSON modules standardized using with { type: 'json' }.
 *
 * RUN:
 * node src/11_modules/04_Import_Attributes.js
 *
 * NOTE:
 * Important statements are explained inline so this file can be used for
 * revision, live coding practice, and interview screen-sharing.
 */


// Standard syntax example (kept as text so this demo runs in every Node version):
const syntax = `import data from "./data.json" with { type: "json" };`;

console.log(syntax);

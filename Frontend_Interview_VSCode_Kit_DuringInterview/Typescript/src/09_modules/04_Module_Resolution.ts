/**
 * INTERVIEW ONE-LINER:
 * moduleResolution controls import lookup; use Bundler with frontend bundlers or NodeNext when Node resolves modules itself.
 *
 * RUN: npx tsx src/09_modules/04_Module_Resolution.ts
 * Important code lines are commented for interview revision.
 */

console.log({bundler:"Vite/Webpack/Rollup",nodenext:"Node ESM/CJS rules"});

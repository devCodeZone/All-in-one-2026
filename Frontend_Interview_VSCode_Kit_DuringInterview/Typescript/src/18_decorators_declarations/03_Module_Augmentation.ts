/**
 * INTERVIEW ONE-LINER:
 * Module augmentation extends an existing module’s declared types without replacing its runtime implementation.
 *
 * RUN: npx tsx src/18_decorators_declarations/03_Module_Augmentation.ts
 * Important code lines are commented for interview revision.
 */

// Example: declare module "./user" { interface User { lastLogin?: Date } }
console.log("Useful for library/framework type extension.");

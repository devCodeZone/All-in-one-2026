/**
 * INTERVIEW ONE-LINER:
 * declare describes runtime values TypeScript cannot see and emits no JavaScript implementation.
 *
 * RUN: npx tsx src/18_decorators_declarations/02_Declare_Global.ts
 * Important code lines are commented for interview revision.
 */

// Example: declare const BUILD_VERSION: string;
console.log("Use declare only when runtime implementation exists elsewhere.");

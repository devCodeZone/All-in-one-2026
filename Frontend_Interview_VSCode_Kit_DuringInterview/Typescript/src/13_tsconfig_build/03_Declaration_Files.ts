/**
 * INTERVIEW ONE-LINER:
 * declaration emit creates .d.ts API descriptions so TypeScript consumers can type-check compiled JavaScript libraries.
 *
 * RUN: npx tsx src/13_tsconfig_build/03_Declaration_Files.ts
 * Important code lines are commented for interview revision.
 */

export function add(a:number,b:number){return a+b} console.log(add(2,3));

/**
 * INTERVIEW ONE-LINER:
 * target controls emitted JavaScript syntax while lib controls which runtime API types TypeScript assumes exist.
 *
 * RUN: npx tsx src/13_tsconfig_build/02_Target_vs_Lib.ts
 * Important code lines are commented for interview revision.
 */

console.log({target:"emitted JS syntax",lib:"available standard/browser API types"});

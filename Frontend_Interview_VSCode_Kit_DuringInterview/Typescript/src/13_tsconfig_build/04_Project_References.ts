/**
 * INTERVIEW ONE-LINER:
 * Project references split large codebases into buildable dependency units and improve incremental build/editor scalability.
 *
 * RUN: npx tsx src/13_tsconfig_build/04_Project_References.ts
 * Important code lines are commented for interview revision.
 */

console.log({apps:["web"],packages:["ui","utils","api-types"],benefit:"explicit build boundaries"});

/**
 * INTERVIEW ONE-LINER:
 * TypeScript catches many compile-time mistakes, but runtime correctness still needs validation, tests, error handling, and security controls.
 *
 * RUN: npx tsx src/19_senior_interview/05_Compile_vs_Runtime.ts
 * Important code lines are commented for interview revision.
 */

console.log({compileTime:"type compatibility",runtime:"network data, user input, permissions, exceptions"});
